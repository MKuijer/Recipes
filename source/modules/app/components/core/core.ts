import { Component } from '@angular/core';
import { ViewEncapsulation } from '@angular/core';

@Component({
    selector: 'core',
    templateUrl: 'core.html',
    encapsulation: ViewEncapsulation.None,
    styleUrls : ['../../../../global-styling.scss']
})

export class Core { }
